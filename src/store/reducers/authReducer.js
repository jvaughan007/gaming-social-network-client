const initState = {
  user: null,
  isLoaded: false
};

const authReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReducer;
